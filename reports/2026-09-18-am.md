✅ 물어오리 AM 정상 발행 · ⚠️ 카드뉴스 신규 원인 확인 — 거짓403 아닌 실제 Meta 이미지 fetch 오류로 9/16·9/17 연속 미발행

## 물어오리 릴스 (AM)
- 마커: `published/2026-09-18-am` = media id `18123632384492971` (정상, pending 아님)
- `containers/2026-09-18-am` = `17906564157444627`, `data/2026-09-18-am.json` 정상 생성
- 커밋 흐름: `reels: 2026-09-18-am` → `reels build: 2026-09-18-am` → `reels publish: 2026-09-18-am` → `metrics: 2026-09-18 인사이트 스냅샷` — 데이터→빌드→게시→메트릭 순차 성공
- GitHub Actions `reels` 워크플로우(run 35280891993, 07:13 KST) conclusion=success 교차 확인
- `selection.signalSources`: `trends`/`nate` 2개 fail, `news_google_rss`는 정상 — 9/17(3개 전부 실패)보다 완화되어 9/16 이전 패턴으로 복귀. 폴백으로 게시 자체는 정상 완료, 별도 조치 불필요
- 이상 징후 없음

## AI뉴스 카드뉴스
- **9/18분**: `digest/2026-09-18`(actions-relay) 생성 완료 — 브리핑 파싱까지 정상. `cards/2026-09-18/` 디렉토리·`published/2026-09-18` 마커는 아직 없음. 과거 렌더 실행 창(09:17~09:44 KST)을 감안하면 현재 점검 시각(08:51 KST)은 그 이전이라 "정상 대기" 범주 — 09:45 KST 이후에도 안 보이면 실패로 재확인 필요
- **9/16·9/17분 원인 특정(전일 보고서 갱신)**: 전일 09/17-am 보고서는 "briefing-cards에 커스텀 Actions 워크플로우가 없어 원인 확인 불가"로 유보했으나, 실제로는 `cardnews.yml`/`watchdog-cardnews.yml` 워크플로우가 **daily-briefing 리포**에 있음을 확인. 두 날짜 모두 렌더·호스팅(briefing-cards push)까지는 성공했으나 `publish.mjs` 단계에서 동일한 Meta API 오류로 실패:
  - `API 400 /***/media`: `code=9004, error_subcode=2207052` — "Media download has failed. The media URI doesn't meet our requirements." (GitHub Pages에 갓 배포된 이미지를 Meta가 못 읽어옴)
  - 이는 기존에 알려진 **거짓 403(code 4 / subcode 2207051, 실게시는 성공하는 버그)과는 다른 오류**임 — publish.mjs가 URL 최신화까지는 확인(`URL N개 전부 최신 이미지 확인`)했지만 이후 Meta 쪽 media 생성 자체가 400으로 거부됨. 즉 **실게시 자체가 안 된 진짜 실패**(마커 없음과 일치)
  - `watchdog-cardnews`가 9/17 01:35 KST에 "오늘 카드뉴스 미게시" 텔레그램 경보 정상 발송함(복구 커맨드 `gh workflow run cardnews.yml -f dry_run=0` 안내) — 감시 체계는 정상 작동 중이나 자동 복구는 없음
  - 2영업일 연속 동일 오류로 발생 — 일시적 CDN 지연이 아니라 재현되는 패턴일 가능성. **사람 확인 필요**: (1) GitHub Pages 배포~Meta fetch 간 캐시/지연 여부, (2) publish.mjs의 URL 대기(현재 12회 재시도) 로직이 "페이지가 200으로 응답"까지만 확인하고 Meta 크롤러가 실제로 읽을 수 있는지는 별도 검증 안 함 — 재시도 로직 보강 또는 수동 워치독 복구 커맨드 실행 검토
- **거짓 403(code 4/2207051) 상태**: 이번 2건의 실패는 이 버그가 아니므로 별도 재현 없음. 마지막 확인된 정상 발행(9/15, `published(pending)`→`published(done)` 6초 간격, 재시도 없이 즉시 성공)을 기준으로 **거짓403 자체는 해소 추정 유지**. 다만 9/16 이후로는 실게시가 아예 안 되고 있어 재현 여부를 판단할 새 데이터가 없음

## daily-briefing
- `collect: 2026-09-18 raw sources`, `briefing: 2026-09-18` → `briefing: 2026-09-18 (telegram-failed)`
- 브리핑 텔레그램 전송 실패가 9/11부터 계속 반복 중, 오늘도 동일 패턴(반복 지적 사항). 릴스·카드뉴스 발행과는 무관

## 요약
- 물어오리 AM 정상 발행, 추가 조치 불필요.
- 카드뉴스: 9/16·9/17분 미발행의 실제 원인(Meta code 9004/2207052, 거짓403과 무관한 별개 오류)을 특정함 — 2일 연속 재현이라 사람의 직접 점검 권장. 9/18분은 아직 렌더 대기 중, 09:45 KST 이후 재확인 필요.
- 브리핑 텔레그램 전송 실패 지속 — 사람 확인 필요(반복 지적).
