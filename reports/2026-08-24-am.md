⚠️ 카드뉴스 미발행 — 브랜드 규칙 검증 실패로 렌더 전 단계에서 중단 (릴스는 정상 발행)

## 물어오리 릴스 (daily-news-reels, am)
- 마커: `published/2026-08-24-am` = `17933492403121261` (media id) → **발행 성공**
- `data/2026-08-24-am.json` 존재 → 데이터 생성 정상
- 커밋 흐름 정상: `reels: 2026-08-24-am`(22:15 UTC) → `reels build`(22:19) → `reels publish`(22:23)
- 이상 없음, 권장 조치 없음

## AI뉴스 카드뉴스 (briefing-cards / daily-briefing cardnews.yml, 2026-08-24)
- 마커: `published/2026-08-24` **없음** → **미발행**
- `cards/2026-08-24/` **없음**(렌더 미실행), `digest/2026-08-24`는 존재(브리핑 파싱·릴레이는 정상)
- 원인 확정: `daily-briefing` repo의 `cardnews.yml` run #54(2026-08-23 22:42 UTC = 08-24 07:42 KST)가 **"브랜드 규칙 검증"** 단계에서 실패, 이후 렌더·게시 스텝은 전부 skip됨.
  - `validate: FAIL (위반 2건)`
    1. `[signal[4]]` 슬라이드 타이틀 35자(기준 34자) 초과: `"book-to-skill: PDF → Claude Code 스킬"`
    2. `[signal[2]]` 비교 대상 `"effort"`가 당일 `raw.json`에 없음 — 학습 기억 기반 환각 의심(원문 소스에 없는 비교를 삽입)
  - 실패 시 텔레그램 알림 정상 발송됨("카드뉴스 파이프라인 실패: 2026-08-24")
- `watchdog-cardnews`(daily-briefing, run #32, 08-24 08:44 KST)도 IG API로 금일 게시물 부재를 확인하고 별도 텔레그램 경보를 정상 발송함(복구 명령 포함: `env -u GITHUB_TOKEN gh workflow run cardnews.yml -R calintzy/daily-briefing -f dry_run=0`). 즉 텔레그램으로 이미 2건의 알림이 갔을 것으로 보임.
- **권장 조치(사람이 할 일)**: 콘텐츠 수정이 필요해 자동 재시도로는 해결되지 않음.
  1. 해당 슬라이드 제목을 34자 이내로 축약
  2. `raw.json`에 없는 "effort" 비교 문구를 원문 근거로 교체하거나 제거
  3. 수정 후 위 복구 명령으로 `cardnews.yml` 재실행

## 카드뉴스 거짓 403 이슈 (Meta 버그, 7/31~) 상태: **해소 추정**
- 7/31 발행분(`08-01` 게시) 로그에서 `media_publish 에러 응답: API 403 ... code:4, error_subcode:2207051` 확인 — 당시엔 "에러 응답에도 실게시 확인 — 성공 처리"로 정상 마무리됨.
- 반면 가장 최근 정상 발행인 8/22·8/23 게시 로그에는 403 에러 라인이 전혀 없이 곧바로 "게시 완료"로 종료됨 — 최근 며칠간은 재현되지 않는 것으로 보여 해소 가능성이 있음.
- 다만 오늘(8/24)은 검증 단계에서 막혀 게시 자체를 시도하지 못했으므로, 오늘 기준으로 403 재현 여부는 확인 불가.

## daily-briefing (브리핑 파이프라인)
- `collect: 2026-08-24 raw sources`(04:44 KST) → `briefing: 2026-08-24`(06:46 KST) → `briefing: 2026-08-24 (telegram-failed)`(06:49 KST) → `digest: 2026-08-24 (actions-relay)`(06:49 KST, briefing-cards로 정상 릴레이) — 데이터/검증 게이트까지는 정상 흐름.
- `(telegram-failed)` 접미사는 브리핑 자체 텔레그램 알림 실패 흔적으로 카드뉴스 발행과는 무관. 최근에도 간헐적으로 반복 발생 중(8/20~8/22 3일 연속 등 기존 보고 이력 있음) — 별도 모니터링 필요.
