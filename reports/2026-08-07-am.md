⚠️ 물어오리 am 미발행 + 카드뉴스 미착수 — GitHub Actions 예약/트리거 장애로 추정 (8/6 오후 UTC부터 지속)

## 물어오리 릴스 (daily-news-reels) — 2026-08-07-am
- 마커: `published/2026-08-07-am` 없음, `containers/2026-08-07-am` 없음 → **빌드·게시 모두 미실행**
- `data/2026-08-07-am.json`은 존재(07:20 KST 커밋 `742e9ef`, 이어 07:20:56 KST에 머지 커밋 `825b09a`) — 데이터 생성 자체는 정상
- **Actions `reels` 워크플로우가 아예 발동하지 않음**: 기록상 가장 최근 `reels` run은 전날 08-06 08:19 UTC(pm 빌드, run 31084399665)이고, 08-07-am 데이터 푸시(22:20 UTC) 이후로는 `reels` run이 하나도 없음. 같은 푸시 직후 `pages build and deployment`는 22:21:06 UTC에 정상 발동돼 push 이벤트 자체는 GitHub에 도달했음이 확인됨 → path-filter(`data/*.json`) 트리거 계층에서만 누락된 것으로 보임
- **안전망(watchdog)도 미작동**: `watchdog.yml`은 08:30 KST(23:30 UTC)에 am 회차를 점검·자동 재dispatch하도록 되어 있으나, 점검 시각(09:00 KST) 기준 실행 기록도 in_progress 상태도 없음 — 마지막 성공 실행은 전날 pm 체크(08-06 20:21 KST)
- 원인 추정: GitHub Actions가 8/6 15:00 UTC경부터 광범위한 지연/미발동 장애를 겪은 것으로 보임(웹 확인). daily-briefing 리포에서도 08-06 05:13 UTC 이후 모든 워크플로우 실행 기록이 끊긴 동일 패턴 관찰(아래) — 이 리포만의 문제가 아닌 계정/플랫폼 레벨 장애 가능성
- 권장 조치: (1) GitHub Actions 상태 정상화 확인 후 `reels.yml`을 `date=2026-08-07-am, dry_run=0`으로 수동 dispatch, (2) `watchdog.yml`도 정상 재개되는지 확인 — 다음 pm 체크(18:00 KST)까지 스스로 안 돌면 수동 개입 필요

## AI뉴스 카드뉴스 (briefing-cards / daily-briefing `cardnews`) — 2026-08-07
- `cards/2026-08-07/`, `digest/2026-08-07`, `published/2026-08-07` **전부 없음 — 파이프라인 미착수**
- daily-briefing: `briefing: 2026-08-07`, `briefing: 2026-08-07 (telegram-failed)` 커밋(06:52~06:54 KST)은 존재하나 `collect: 2026-08-07 raw sources` 커밋이 없음 — 정규 `collect.yml` 경로가 아닌 별도 루틴으로 브리핑이 생성된 것으로 보임
- daily-briefing Actions 기록: 08-06 05:13 UTC(`comment` 워크플로우) 이후 `collect`·`telegram-digest`·`cardnews`·`watchdog-cardnews` 어떤 워크플로우도 실행되지 않음 — reels와 동일한 장애 시간대와 일치
- `cardnews.yml`은 07:20 KST(22:20 UTC) 예약 실행인데 이번에도 발동 기록 없음
- 권장 조치: Actions 정상화 후 `cardnews.yml` workflow_dispatch(dry_run=0)로 수동 실행. daily-briefing `watchdog-cardnews`도 함께 재개 여부 확인

## 카드뉴스 거짓 403 — 지속 여부
- 8/5 23:27 KST 적용된 Graph API `v22.0` 고정 수정(`25d9cca`, "내일 실행에서 에러 없이 게시되면 원인 확정")의 검증 결과: 08-06 발행 실행(run 31056336266, 08-05 23:26~23:28 KST) 로그에서 `media_publish 에러 응답: API 403 code:4 subcode:2207051` 재현 확인 → **v22.0 고정으로도 해소되지 않음, 거짓 403 지속 중(7/31~, 8일째)**
- 오늘(08-07) 회차는 카드뉴스 파이프라인 자체가 아직 실행되지 않아 재현 여부 미확인 — 파이프라인 재개 시 재확인 필요
- `publish.mjs`의 실게시 사후검증 로직은 정상 동작 중이므로, 파이프라인이 도는 한 발행 자체는 문제없음(마커에 media id 기록됨)

---
점검 시각: 2026-08-07 09:00 KST
