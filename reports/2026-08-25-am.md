# 2026-08-25 아침 점검

✅ 전 회차 정상 발행 (릴스 am + 카드뉴스). 단, 브리핑 생성이 로컬 폴백 경로로 처리됨 — 발행 자체엔 영향 없음, 반복 패턴이라 원인 확인 권장.

## 물어오리 릴스 (daily-news-reels, am)
- 마커: `published/2026-08-25-am` = `18617908921026955` (media id) → **발행 성공**
- `data/2026-08-25-am.json`, `containers/2026-08-25-am` 존재 → 데이터 생성·컨테이너 생성 정상
- 커밋 흐름 정상: `reels: 2026-08-25-am`(22:15 UTC) → `reels build`(22:20) → `reels publish`(22:24)
- Actions: `reels`, `pages build and deployment` 모두 success. `watchdog`(09:41 KST)도 success로 이상 없음 확인.
- 이상 없음, 권장 조치 없음

## AI뉴스 카드뉴스 (briefing-cards / daily-briefing cardnews.yml, 2026-08-25)
- 마커: `published/2026-08-25` = `18098162501618361` (media id) → **발행 성공**
- `cards/2026-08-25/` 슬라이드 6장 렌더 완료, `digest/2026-08-25` 존재(브리핑 파싱·릴레이 정상)
- `cardnews.yml` run #56(22:47~22:50 UTC) 전 스텝 success. `validate: PASS` 확인.
- 게시 스텝 로그 직접 확인: 403 에러 없이 아이템 컨테이너 6개 생성 → 캐러셀 게시 완료. 총 소요 약 90초는 CDN 이미지 URL 전파 대기 재시도 5회(`URL 대기 1/12~5/12: HTTP 404 재시도`) 때문이며 정상 동작 패턴.
- 이상 없음

## 카드뉴스 거짓 403 이슈 (Meta 버그, 7/31~) 상태: **해소 지속**
- 오늘(8/25) publish 로그에 `code:4, subcode:2207051` 등 403 에러 라인 전혀 없음 — 8/22 이후 재현 사례 없이 계속 정상 게시 중.

## daily-briefing (브리핑 파이프라인)
- `collect: 2026-08-25 raw sources`(04:53 KST, `collect.yml`, success) → `briefing: 2026-08-25 (fallback)`(07:11 KST) — collect와 briefing 생성 사이 약 2시간18분 공백.
- `(fallback)` 태그: 커밋에 포함된 브리핑 md 하단에 "로컬 launchd 폴백 환경"이라는 자기 기술이 있음 — 정규 생성 경로가 시간 내 실행되지 않아 로컬 폴백이 대신 처리한 것으로 추정. 동일 패턴이 8/23에도 있었음(`031995f briefing: 2026-08-23 (fallback)`) — **2회 반복**.
- 폴백 환경 제약으로 함께 발생한 부수 실패(발행과는 무관, 참고용):
  - Reddit(r/ClaudeAI 등) 수집 실패 — 데이터센터 IP 403 차단
  - star_history 기록 스킵 — GITHUB_TOKEN 미보유
  - 로컬 폴백 자체의 텔레그램 전송 시도는 TG_BOT_TOKEN 미보유로 미시도 — 다만 push를 받은 GitHub Actions `telegram-digest.yml`(22:11 UTC, success)이 정상 릴레이하여 최종 텔레그램 발송 자체는 성공. `(telegram-failed)` 접미사는 오늘 커밋에 없음.
- 카드뉴스 발행에는 영향 없음: 폴백이 생성한 `briefings/2026-08-25.md`·`cardnews/src/2026-08-25.json`으로 파싱→검증→렌더→게시 전 단계 정상 진행됨.

## 권장 조치 (사람이 할 일)
- 브리핑 생성 정규 경로가 8/23, 8/25 두 차례 시간 내 실행되지 않아 로컬 launchd 폴백에 의존한 것으로 보임 — 정규 경로(스케줄/세션)의 헬스 확인 권장. 폴백 상태가 굳어지면 Reddit 수집·star_history 등 부가 기능이 계속 누락됨.
- (참고, 이번 점검 범위 밖) `ai-2026-08-24`(daily-news-reels 별도 산출물)는 8/24 데이터 검증 실패로 여전히 미발행 상태 — 전일 pm 보고서에 이미 반영, 추가 재시도 없음.
