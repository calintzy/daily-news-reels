⚠️ 릴스 정상 발행 / 카드뉴스는 아직 미발행(정상 소요시간 내로 판단, 이상 아님)

**물어오리 릴스 (daily-news-reels) — am**
- `published/2026-09-24-am` = `18105580456961010` (media id) → 정상 발행 ✅
- 발행 시각: 2026-09-23 22:31:35 UTC (2026-09-24 07:31 KST)
- `data/2026-09-24-am.json`, `containers/2026-09-24-am` 모두 존재 — 데이터 생성부터 게시까지 전 단계 정상

**AI뉴스 카드뉴스 (briefing-cards) — 2026-09-24**
- `published/2026-09-24`, `cards/2026-09-24`, `digest/2026-09-24` 모두 아직 없음
- daily-briefing 쪽은 정상: `briefing: 2026-09-24` 커밋(06:48 KST)으로 `briefings/2026-09-24.md` 이미 존재 → cardnews.yml 게이트 통과 가능한 상태
- cardnews 워크플로우 cron은 07:20/08:05 KST이지만, 최근 4일(9/20~9/23) 실측으로는 스케줄 지연(~2~2.5시간)이 있어 실제 렌더+게시는 09:40~09:45 KST경 발생. 현재(08:52 KST) 시점은 통상 소요시간 내이므로 "미발행"이 아니라 "아직 실행 전"으로 판단됨 → 저녁 점검 또는 재확인 필요, 09:45 KST 이후에도 마커가 없으면 진짜 실패로 재분류할 것.

**이상 징후 (참고, 발행 차단 아님)**
- telegram-digest 세이프티넷 워크플로우(21:48 UTC 실행, conclusion=success)의 실제 로그: "클라우드 1차 전송 성공한 날 — 스킵". 그러나 같은 날짜의 daily-briefing 커밋은 `briefing: 2026-09-24 (telegram-failed)`로 텔레그램 전송 실패를 기록 — 두 판정이 모순됨. cardnews 렌더는 `digest/` 마커가 아니라 `briefings/*.md`를 직접 읽으므로 카드뉴스 발행 자체는 영향받지 않을 것으로 보이나, 세이프티넷의 "성공 판정" 로직(scripts/telegram_digest.py)이 실패를 잘못 감지하는 버그일 가능성 있음 — 점검 권장.
- 텔레그램 브리핑 전송 실패가 9/22, 9/23, 9/24 3일 연속 발생(`(telegram-failed)` 커밋 3회 연속). 발행 파이프라인과는 별개지만 반복 패턴이므로 TG_BOT_TOKEN/TG_CHAT_ID 등 원인 점검 권장.

**daily-briefing 커밋 흐름**: collect → briefing → (telegram-failed 안전망 시도) 순으로 정상 진행 중. 이상 없음.

**카드뉴스 거짓 403(subcode 2207051) 상태**: 8/5 Graph API 버전 고정(v22.0) 조치 이후 최근 4일(9/20~9/23) `published(pending)`→`published(done)` 전환이 모두 4~6초로 빠르고 깨끗하게 완료됨(재시도·검증 우회 흔적 없음) → **재발 징후 없음, 해소된 것으로 추정**. 계속 모니터링 필요.

**권장 조치**
1. 09:45 KST 이후 `briefing-cards/published/2026-09-24` 재확인 — 없으면 cardnews 워크플로우 로그 확인.
2. `scripts/telegram_digest.py`의 "클라우드 1차 전송 성공" 판정과 `(telegram-failed)` 마커 불일치 원인 점검.
3. 텔레그램 브리핑 전송 3일 연속 실패 원인 점검(토큰 만료 등).
