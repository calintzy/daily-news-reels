# 2026-08-26 아침 점검

⚠️ 물어오리 릴스 am 회차 미발행 — 데이터 검증 실패로 게시 중단(자동 재시도 1회 포함 총 2회 동일 실패). 카드뉴스는 정상 발행.

## 물어오리 릴스 (daily-news-reels, am)
- 마커: `published/2026-08-26-am` **없음** → 미발행
- `data/2026-08-26-am.json` 존재(데이터 생성 정상) / `containers/2026-08-26-am` **없음** → 이미지 생성 이전 단계(검증)에서 중단
- 커밋 흐름: `reels: 2026-08-26-am`(2026-08-25 22:21:55Z push) → `reels` run #108(22:22 UTC) 트리거
- run #108 `build` job, "데이터 검증" 스텝에서 FAIL:
  ```
  validate: FAIL (위반 1건)
  [구조] issue[3].summary 124자 > 120자
  ```
  → 이미지 생성·렌더·산출물 커밋·게시 스텝 전부 skip, Telegram 알림 발송(message_id=392) 후 종료.
- `watchdog` run #77(23:44 KST, schedule)이 마커 없음을 감지 → 자가복구 로직이 데이터 존재 확인 후 build+publish 재dispatch(run #109, 23:45)
- run #109도 **동일한 검증 스텝에서 동일 사유로 재실패**(issue[3].summary 124자, 데이터 파일이 그대로였으므로 예상된 결과) → watchdog은 "미발행이면 run을 실패로 표시" 정책에 따라 자신도 failure로 종료, 경보 텔레그램 전송 완료.
- **원인 추정**: 데이터 생성 단계(요약 생성 로직) 자체는 정상 동작했으나, `issue[3].summary` 필드가 검증 상한(120자)을 4자 초과하여 검증 게이트에서 반려됨. 이미지·렌더·게시 로직에는 문제 없음 — 순수 데이터 콘텐츠 길이 초과.

## AI뉴스 카드뉴스 (briefing-cards / daily-briefing cardnews.yml, 2026-08-26)
- 마커: `published/2026-08-26` = `18126387532703835` (media id) → **발행 성공**
- `cards/2026-08-26/` 슬라이드 6장 렌더 완료, `digest/2026-08-26` 존재(브리핑 파싱·릴레이 정상)
- `cardnews` run #57(22:48~22:51 UTC, daily-briefing) 전 스텝 success. 파싱·검증·렌더·호스팅·게시 전부 정상.
- 게시 로그 직접 확인: `code:4, subcode:2207051` 등 403 에러 라인 없음. CDN 이미지 URL 대기 재시도 5회(`URL 대기 1/12~5/12: HTTP 404 재시도`) 후 아이템 컨테이너 6개 생성 → 캐러셀 게시 완료(93초 소요) — 정상 동작 패턴.

## 카드뉴스 거짓 403 이슈 (Meta 버그, 7/31~) 상태: **해소 지속**
- 오늘(8/26) publish 로그에도 403/code 4/subcode 2207051 라인 전혀 없음 — 8/22 이후 재현 사례 없이 계속 정상 게시 중.

## daily-briefing (브리핑 파이프라인)
- `collect: 2026-08-26 raw sources`(19:54 KST) → `briefing: 2026-08-26`(21:49 KST) → `briefing: 2026-08-26 (telegram-failed)`(21:51 KST) 순으로 정상 흐름.
- `(telegram-failed)` 접미사: 브리핑 자체의 텔레그램 전송 실패 흔적. 카드뉴스 발행과는 별개 경로이며, 카드뉴스는 위와 같이 정상 발행됨 — 영향 없음.

## 권장 조치 (사람이 할 일)
1. **[긴급] 릴스 am 회차 미발행**: `data/2026-08-26-am.json`의 `issue[3].summary`를 120자 이하로 축약한 뒤, `daily-news-reels`의 `reels.yml`을 해당 stem(`2026-08-26-am`)으로 수동 workflow_dispatch 재실행 필요. watchdog의 자동 재시도가 이미 1회 소진되어 추가 자동 복구는 없음.
2. 재발 방지: 데이터 생성 단계의 요약(summary) 생성 로직·프롬프트가 120자 제한을 안정적으로 지키도록 점검 권장(이번 건은 4자 초과로 근소한 초과).
3. `briefing: 2026-08-26 (telegram-failed)` — 브리핑 텔레그램 전송 실패가 반복되는지 추이 확인 권장(카드뉴스 발행에는 영향 없음, 별도 채널 이슈로 추정).
