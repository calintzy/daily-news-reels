⚠️ 릴스(pm) 발행 실패 — Meta 컨테이너 처리 단계에서 ERROR, 마커 미기록

## 물어오리 릴스 (pm)

- 마커: `published/2026-08-17-pm` **없음** (발행 실패), `containers/2026-08-17-pm` **없음** (커밋 스킵)
- `data/2026-08-17-pm.json` 존재 (커밋 `e028444`, 08:21:40 UTC = 17:21:40 KST) — 데이터 생성 정상
- git log: `reels: 2026-08-17-pm` → `reels build: 2026-08-17-pm`(커밋 `0e6db49`, 17:27:16 KST, 이미지·영상 렌더 정상, `docs/videos/2026-08-17-pm.mp4` 포함)까지는 정상 완주. **`reels publish: 2026-08-17-pm` 커밋 없음, `report:` 커밋도 이번이 처음(본 보고서)** — 게시 단계에서 체인 중단
- Actions run `32010005727`(reels, push, conclusion=failure, 17:21:48 KST):
  - `build` job: 성공 (17:21:51→17:27:28 KST)
  - `publish` job: **실패** (17:27:31→17:32:43 KST)
    - GitHub Pages에 올라간 mp4 URL 프리플라이트가 8회 재시도(404) 끝에 17:32:22 KST에야 OK로 확인됨 — Pages 배포 지연 자체는 평소 패턴과 유사해 근본 원인 아님
    - 이후 Meta IG 컨테이너 생성(`17899510761444627`)까지는 성공, 상태 폴링에서 `IN_PROGRESS` → **`ERROR`**로 전이 → `publish.mjs`가 예외 던지고 종료코드 1
    - 스크립트가 Meta 응답의 `status_code`/`status`/`id`만 로깅하고 상세 에러 객체(`error.message`, `error_subcode` 등)는 기록하지 않아 **정확한 실패 사유는 로그만으로 특정 불가**
    - 실패 직후 텔레그램 알림 발송됨(`message_id=279`) — 담당자에게는 이미 실시간 통지된 상태로 추정
  - `publish` 실패로 인해 멱등 마커 커밋 단계(`containers/`, `published/`)가 스킵됨 → 리포에는 실패 흔적만 남고 마커는 비어 있음
  - 이후(17:32 KST~현재 17:55 KST 시점) watchdog 등 재시도 워크플로우 재실행 없음 — 자동 재발행 없이 그대로 방치된 상태

- **참고**: 이번 실패는 알려진 카드뉴스 `media_publish` 거짓 403(7/31~ 지속) 패턴과 다름 — 그건 실게시는 성공하고 API 응답만 거짓 403인 케이스인데, 이번 건은 컨테이너 자체가 Meta 쪽에서 `ERROR` 상태로 확정되어 **실제로 게시되지 않은 것으로 판단됨** (컨테이너 id는 발급됐으나 media id 획득 못함)

- **권장 조치(사람 확인 필요)**:
  1. Meta 개발자 콘솔 또는 그래프 API로 컨테이너 `17899510761444627` 상태·에러 상세 직접 조회 (스크립트 로그에 없는 원인 확인)
  2. 영상 자체(`docs/videos/2026-08-17-pm.mp4`, 23.4MB) 포맷/코덱/길이 이상 여부 점검 — 같은 날 am 회차는 정상 발행되어 파이프라인 자체 문제 가능성은 낮고, 이 영상 파일 개별 이슈 또는 Meta 측 일시 오류 가능성
  3. 원인 확인 후 수동으로 pm 회차 재게시 트리거 필요 (빌드 산출물은 이미 존재하므로 publish 단계만 재실행하면 될 것으로 보임)
  4. 재발 방지 차원에서 `publish.mjs`가 Meta 에러 응답의 상세 필드까지 로깅하도록 개선 검토 권장

## daily-briefing (참고)

- 오늘 커밋 흐름: `collect: 2026-08-17 raw sources`(04:42 KST) 이후 `briefing: 2026-08-17` 커밋 없음 — 아침 보고서와 동일 상황 지속. pm 회차(릴스)는 daily-briefing과 무관한 별도 데이터 소스라 이번 실패와는 직접 연관 없음
- 카드뉴스는 아침 1회 발행 대상이라 이번 저녁 점검 대상 아님(스킵)
