⚠️ 물어오리 릴스 pm 회차 미발행 — CI 스템 판정 오류로 검증 단계에서 중단 (데이터 자체는 정상 생성됨)

## 물어오리 릴스 (daily-news-reels) — pm 회차

- **마커**: `published/2026-09-26-pm` 없음 → 미발행
- **데이터**: `data/2026-09-26-pm.json` 존재 (commit `1290ede`, 08:27:46Z, 73줄) → 데이터 생성 정상
- **containers/2026-09-26-pm**: 없음 → 이미지 생성 이전 단계에서 중단됨
- **워크플로우**: `reels.yml` run #209 (`36229861579`) — conclusion `failure`
  - 실패 단계: **"데이터 검증 (실패 시 Telegram 알림 후 중단)"**
  - 이후 이미지 생성/렌더/게시/미리보기 전송 전부 skipped

### 원인 추정
데이터 커밋(`1290ede`, 08:27:46Z) 6초 뒤 `Merge remote-tracking branch 'origin/main'` 커밋(`290dbab`, 08:27:52Z)이 같은 push에 실려 올라감. 이 push의 HEAD가 머지 커밋이 되면서, 스템(회차 키) 판정 스크립트가 쓰는 `git show --name-only --pretty=format: HEAD -- data/`가 **머지 커밋에서는 기본적으로 파일 diff를 반환하지 않음**(2부모 diff는 `-m` 옵션 필요). 그 결과 `CHANGED`가 비어 fallback 경로로 빠져 `date=$(TZ=Asia/Seoul date +%F)` → `"2026-09-26"`(pm 접미사 없음)로 잘못 판정. 존재하지 않는 `data/2026-09-26.json`을 검증하려다 `ENOENT`로 실패, 파이프라인 전체가 중단됨.

Telegram 실패 알림(`node scripts/telegram.mjs fail`)은 정상 전송됨(`sendMessage OK: message_id=785`).

### 권장 조치 (사람이 할 일)
1. **즉시 복구**: 실제 데이터(`data/2026-09-26-pm.json`)는 존재하므로 `reels.yml`을 stem `2026-09-26-pm` 지정 `workflow_dispatch`로 수동 재실행하면 이미지 생성부터 재개 가능.
2. **재발 방지**: 스템 판정 스크립트가 머지 커밋을 못 다루는 구조적 결함. `git show --name-only HEAD`를 `git diff-tree --no-commit-id --name-only -r -m HEAD`(머지 커밋도 처리) 또는 `github.event.before..after` 범위 조회로 교체 필요. 데이터 생성 세션이 push 전에 `origin/main`을 pull/merge하는 습관이 있어 향후에도 재발 가능성 있음.

## 참고: daily-briefing / AI 릴스
- 오늘 오전 별도 트랙인 "ai reels" (`ai-2026-09-26`)는 03:09~03:17 정상 빌드·게시됨 — 물어오리 pm과는 무관한 별개 파이프라인.
- daily-briefing 커밋 흐름(9/25 21:51~22:35): `briefing: 2026-09-26` → `briefing: 2026-09-26 (telegram-failed)` → `collect: 2026-09-26 raw sources`. 브리핑 텔레그램 발송 실패 흔적 있으나 발행과는 별개 이슈.

## 참고: 카드뉴스
저녁 회차이므로 이번 점검 대상 아님(카드뉴스는 아침 1회 발행).
