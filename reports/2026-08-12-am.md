# 2026-08-12 아침 발행 점검

⚠️ 물어오리 릴스 am 미발행 — 데이터 JSON 파싱 오류로 빌드 중단 (카드뉴스는 정상 발행)

## 물어오리 릴스 (am)
- 마커: `published/2026-08-12-am` 없음 (미발행)
- `data/2026-08-12-am.json` 생성됨(07:21 KST, 커밋 `reels: 2026-08-12-am`) — 그러나 JSON 문법 오류로 무효
  - 원인: 56번째 줄 `sourceTitle` 필드 값 안에 이스케이프 안 된 큰따옴표 포함 — `"네이버페이 단말기-PG '리베이트 논란'…금감원 "특이사항 없어"일축"` (원문 기사 제목의 내부 인용부호를 그대로 삽입해 JSON 문법이 깨짐, char position 3533)
  - `validate.mjs`가 파싱 오류를 감지해 빌드 중단 → Actions `reels` run 실패(run 31542030583, 07:22:30 KST) + 텔레그램 실패 알림 자동 발송(07:22:37 KST, message_id=225)
- `containers/2026-08-12-am` 없음 — 검증 단계에서 막혀 빌드 자체가 시작되지 않음
- watchdog: 08:30 KST 자동 점검(cron)이 이 보고서 작성 시점(08:52)까지 미실행(GH cron 지연 가능). 실행되더라도 데이터가 그대로면 `reels.yml`을 재dispatch해도 동일 사유로 재실패할 것으로 예상됨

## AI뉴스 카드뉴스
- 마커: `briefing-cards/published/2026-08-12` = `17868249852650397` (media id, 성공)
- `cards/2026-08-12/` 7장 렌더 완료, `digest/2026-08-12` 존재 — 파싱·렌더·게시 전 단계 정상
- Actions `cardnews` run(daily-briefing repo): 2026-08-11T23:07:22Z (KST 08:07) completed/success
- **거짓 403 지속 중**: `media_publish` 단계에서 `code=4, subcode=2207051(Application request limit reached)` 응답 발생(08:09:47 KST). publish.mjs가 실게시 검증 후 media id 확인 → 성공 처리(08:09:57 KST). 7/31부터 이어지는 Meta 측 버그가 8/5 Graph API 버전 고정 실험 이후에도 8/12 현재까지 계속 재현됨. 워크어라운드는 정상 동작 중이라 발행 자체엔 영향 없음.

## daily-briefing 참고
- `collect: 2026-08-12`(20:08:59) → `briefing: 2026-08-12`(21:51:19) → 재시도 `briefing: 2026-08-12 (telegram-failed)`(21:54:56)
- 브리핑 다이제스트 텔레그램 전송 실패가 재발(어제와 동일 패턴). 카드뉴스는 digest를 정상 소비해 렌더·게시에 영향 없음.

## 권장 조치
1. **[필요]** `data/2026-08-12-am.json` 56번째 줄 `sourceTitle` 값 내부의 이스케이프 안 된 큰따옴표를 수정(`\"`)한 뒤 `reels.yml`을 `workflow_dispatch`로 재실행(`date=2026-08-12-am`)하거나, 데이터 수정 후 watchdog 자동 재시도를 기다릴 것. 수정 없이는 재시도해도 동일하게 실패함.
2. 데이터 생성 단계(원문 기사 제목을 `sourceTitle`에 옮기는 로직)가 내부 인용부호를 이스케이프하지 않는 근본 버그로 보임 — 재발 방지를 위해 안전한 JSON 직렬화 적용 검토 권장.
3. 카드뉴스 거짓 403은 지속되나 워크어라운드가 안정적으로 동작 중이라 당장 조치 불필요.
