⚠️ 물어오리 pm 릴스 미발행 — Instagram 컨테이너 처리 오류(ERROR)

## 물어오리 릴스 (pm, 2026-09-14)
- **마커**: `published/2026-09-14-pm` 없음 → 미발행
- **데이터**: `data/2026-09-14-pm.json` 정상 생성 (17:19 KST, 이슈 5건 정상 채움). 다만 `selection.signalSources`(trends/nate/google_news)가 전부 `fail` — 트렌드 시그널 수집은 안 됐고 폴백 경로로 진행된 것으로 보임(발행 차단 요인은 아님, 참고용).
- **빌드**: 정상 완료 (17:24 KST 커밋). 영상 `videos/2026-09-14-pm.mp4` 생성·GitHub Pages 배포 확인, 프리플라이트 7회 재시도(404) 끝에 최종 OK (20.8MB).
- **게시(publish) 단계에서 실패**: IG 컨테이너 생성(`17905801488444627`)까지는 성공했으나, 상태 폴링 결과 `ERROR` 반환 →
  `Error: 컨테이너 처리 실패: {"status_code":"ERROR","status":"ERROR","id":"17905801488444627"}` 로 `publish.mjs`가 예외를 던지고 종료(exit 1). GitHub Actions `reels` 워크플로우 실패로 기록됨 (run 34822149129).
- `containers/2026-09-14-pm` 마커 파일도 생성되지 않음(퍼블리시 성공 전 단계에서 중단).
- publish.mjs 자체 실패 알림이 텔레그램으로 이미 발송됨(`sendMessage OK: message_id=634`) — 발행 실패는 실시간으로 통지된 상태.

**주의**: 이 실패는 카드뉴스에서 알려진 "거짓 403(media_publish, code 4/2207051)" 이슈와는 다른 증상입니다. 이번 건은 media_publish 이전 단계인 **IG 컨테이너 자체가 Meta 쪽에서 처리 실패(ERROR) 상태**를 반환한 것으로, 실게시가 이뤄졌을 가능성은 낮습니다(마커 없음 + containers/ 마커도 없음).

## 원인 추정
영상 자체(형식/길이/코덱) 또는 Meta 그래프 API 쪽의 일시적 컨테이너 처리 오류로 추정됩니다. 프리플라이트 단계에서 GitHub Pages CDN 전파 지연(404 재시도 7회)이 있었던 점은 정상 범위이며 컨테이너 ERROR와는 별개로 보입니다.

## 권장 조치 (사람이 할 일)
1. Meta Graph API에서 컨테이너 id `17905801488444627`의 상세 에러 코드를 조회해 근본 원인 확인 (예: `GET /17905801488444627?fields=status_code,status`가 아닌 실패 상세는 보통 이후 조회 시 만료되므로, 재현 시 로그에 에러 상세를 남기도록 publish.mjs 보강 검토).
2. 원인이 일시적(Meta 측 처리 오류)로 판단되면 워크플로우 수동 재실행(re-run) 고려. 영상 자체 문제(형식/길이)면 렌더 파이프라인 점검 필요.
3. 현재 pm 회차는 미발행 상태로 남아 있음 — 재게시 여부 결정 필요.

---
_이 보고서는 점검 전용 에이전트가 작성했습니다. 코드 수정·워크플로우 재실행 등 조치는 취하지 않았습니다._
